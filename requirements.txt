# Python dependencies for the oxidation-resisting NiCoCrAlFe HEA ML pipeline.
# Lower bounds match the versions this project was tested with (Python 3.13).
# Install with:  pip install -r requirements.txt

# ---- Core: required for GBR.ipynb, ModelSelection.ipynb and functions.py ----
pandas>=3.0
numpy>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
optuna>=3.0
joblib>=1.3
openpyxl>=3.1          # pandas engine for reading docs/*.xlsx (not imported directly)

# ---- Optional model backends: only needed if you select that model in ----
# ---- ModelSelection.ipynb via MODEL_NAME ('xgboost' | 'lightgbm' | 'catboost') ----
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# ---- Optional: only for otherModels/ANN.ipynb ----
# torch is NOT needed for GBR.ipynb / ModelSelection.ipynb.
# On Windows its deeply nested files can exceed the 260-char path limit. If
# `pip install torch` fails with that error, enable long-path support once in an
# ADMIN PowerShell, then install the CPU build:
#   New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.2
